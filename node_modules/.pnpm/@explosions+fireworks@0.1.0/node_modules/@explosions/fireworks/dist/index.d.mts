interface MinMax {
    min: number;
    max: number;
}
interface FireworksConfig {
    selector: string;
    colors: string[];
    numberOfParticles: number;
    orbitRadius: MinMax;
    circleRadius: MinMax;
    diffuseRadius: MinMax;
    animeDuration: MinMax;
}
interface Point {
    x: number;
    y: number;
}

/**
 * @see https://codepen.io/juliangarnier/pen/gmOwJX
 * inherited from hexo-theme-yun
 * customized by valaxy-theme-yun
 * @author YunYouJun
 */

/**
 * update pointer
 * clientX not work in iOS
 */
declare function getCoordsFromEvent(e: MouseEvent | TouchEvent): Point;
/**
 * 设置画布尺寸
 * @default
 */
declare function setCanvasSize(canvasEl: HTMLCanvasElement, width?: number, height?: number): void;
/**
 * 创建烟花
 * @param config
 */
declare function createFireworks(config: Partial<FireworksConfig>): void;

export { createFireworks, getCoordsFromEvent, setCanvasSize };
